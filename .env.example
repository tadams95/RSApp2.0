# Firebase App Check Debug Token
# Generate this token in Firebase Console > App Check > Apps > Manage debug tokens
# This token is used for development builds only
EXPO_PUBLIC_APP_CHECK_DEBUG_TOKEN=your-debug-token-here

# PostHog Analytics API Key
EXPO_PUBLIC_POSTHOG_API_KEY=your-posthog-api-key-here

# Stripe Cloud Function URL
EXPO_PUBLIC_STRIPE_API_URL=https://us-central1-ragestate-app.cloudfunctions.net/stripePayment

# Proxy Key for Cloud Functions authentication (required for local dev)
# Must match the PROXY_KEY set in your Firebase Cloud Functions environment
PROXY_KEY=your-proxy-key-here

# Instructions:
# 1. Go to Firebase Console > App Check
# 2. Click on your app (iOS or Android)
# 3. Click "Manage debug tokens"
# 4. Create a new debug token
# 5. Copy the token and paste it here
# 6. Copy this file to .env (which is gitignored)
