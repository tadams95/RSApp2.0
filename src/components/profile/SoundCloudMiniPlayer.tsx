/**
 * SoundCloudMiniPlayer
 *
 * NOTE: This component is deprecated.
 * Audio playback is now handled globally by SoundCloudPlayerContext.
 *
 * The hidden WebView player lives in:
 * src/hooks/SoundCloudPlayerContext.tsx
 *
 * Use the useSoundCloudPlayer() hook to control playback.
 */

export default function SoundCloudMiniPlayer() {
  // No-op - playback is handled by SoundCloudPlayerContext
  return null;
}
